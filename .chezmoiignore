README.md

# このリポジトリは chezmoi ソースと Nix flake / OpenSpec が同居しているため、
# chezmoi が管理してはいけない非 dotfiles を除外する（$HOME への誤展開を防止）。
.gitignore
flake.nix
flake.lock
darwin.nix
home.nix
result
openspec
openspec/**
